#include <bits/stdc++.h>
using namespace std;
#define ll long long int 


int main()
{
	ll n;
	while(scanf("%Ld", &n), n)
	{
		long double raiz = sqrt(n);
		long double ans = raiz - (int)raiz;
		if(!ans) puts("yes");
		else puts("no");
	}	
}
      



